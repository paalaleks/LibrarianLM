---
topic: EPUB to Single-Page HTML Reader
updated: 2026-08-23T08:48
---

- (event) Create run initialized from docs/brief/html-epub-skill-brief.md because the user invoked bmad-prd with that source.
- (decision) PRD stakes calibrated as an internal tool within a larger project, requiring moderate rigor.
- (decision) Fast path selected because the source brief is detailed; inferred gaps are tagged as assumptions.
- (decision) src/epub-html is the canonical and exclusive product subtree, and the existing scripts/template.html is authoritative.
- (assumption) MVP includes linear spine documents and includes non-linear content only when required by an included internal link; needs confirmation.
- (assumption) Obfuscated embedded fonts are discarded in favor of canonical offline system font stacks; needs confirmation.
- (assumption) Hostile-input archive and XML resource limits will be configurable and fail actionably; exact limits remain open.
- (assumption) MVP embeds safe raster images and supported SVG but omits audio, video, and unsupported foreign content; needs confirmation.
- (assumption) MVP has no numeric performance SLA beyond bounded conversion and interactive use on the agreed corpus.
- (assumption) One shared accent and design identity apply to every Reader Artifact in MVP; per-book accents are out of scope.
- (change) Research expanded security requirements to distinguish DRM from font obfuscation, bound hostile package processing, and prevent all external fetches.
- (override) User confirmed EPUB inputs come from a known trusted source; removed hostile-input defenses, adversarial fixtures, CSP, archive-bomb, traversal, and XXE requirements. Retained only brief-mandated source cleanup for deterministic offline output.
- (change) Updated docs/brief/html-epub-skill-brief.md to replace sanitization/security framing with trusted-source offline cleanup and explicitly exclude security-shell work.
- (decision) Preserve EPUB body markup and non-resource attributes by default while omitting all images, multimedia, source styling, embedded resources, and live remote references.
- (decision) Replace the persistent desktop sidebar with a collapsible framework-free left sheet at every viewport; closing it expands the reading region and accessibility state is required.
- (decision) Confirmed one accent, linked-only transitive non-linear inclusion, omission warnings for remote resources, equal-IR malformed recovery, atomic failure on disagreement, and no conversion manifest.
- (change) Reconciled brief, PRD, addendum, and implementation spec; added CLI/summary, normalized IR/serializer, slot/data, inclusion closure, cleanup, fixture, and verification contracts after review.
- (change) Re-baselined spec, brief, PRD, and addendum to updated canonical template: preserve its existing all-width modal sheet and remove the earlier reading-region expansion interpretation; remaining template work is slots, Book Rail, preserved headings, and non-overwriting facts.
- (event) Validate intent: Fair grade; conversion-contract gaps blocking before development (CLI/JSON, parser identity, FR-11 vs FR-16, §11 false closure).
